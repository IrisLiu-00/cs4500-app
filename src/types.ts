export type ArtInfo = {
  thumbnail: {alt_text: string},
  artist_display: string,
  date_display: string,
  id: number,
  image_id: string,
  title: string,
  imageUrl: string,
  updatedAt: Date,
  lines: number,
}